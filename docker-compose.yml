version: "3.9"

services:
  library-manager:
    build: .
    container_name: library-manager

    ports:
      - "5000:5000"

    env_file:
      - .env

    volumes:
      - .:/app

    restart: unless-stopped